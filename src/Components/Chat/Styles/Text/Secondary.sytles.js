import { StyleSheet } from "react-native";
import { Dimensions, Colors } from "../../Themes";
export const styles = StyleSheet.create({
  textStyles: {
    fontSize: Dimensions.TEXT_SIZE_SECONDARY,
    color: Colors.TEXT_SECONDARY_COLOR
  }
});

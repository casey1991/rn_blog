import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  layoutContainer: { position: "relative", flex: 1 },
  layoutFAB: { position: "absolute", bottom: 16, right: 16 }
});
